input {
  file {
    path => "/var/log/fishbase/api.log"
    start_position => "beginning"
  }
}

output {
  stdout {
    codec => rubydebug
  }


  elasticsearch {
    embedded => true
    host => "127.0.0.1"
    port => "9200"
    protocol => "http"
  }
}

