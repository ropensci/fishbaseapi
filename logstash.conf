input {
  file {
    path => "/root/fishbaseapi.log"
    start_position => "beginning"
  }
}

output {
  stdout {
    codec => rubydebug
  }

  elasticsearch {
    embedded => ES_EMBEDDED
    host => "http://server.carlboettiger.info"
    port => "ES_PORT"
    protocol => "ES_PROTOCOL"
  }
}


