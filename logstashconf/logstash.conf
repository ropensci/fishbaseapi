input {
  file {
    path => "/var/log/fishbase/api.log"
    start_position => "beginning"
  }
}

output {
  stdout {
    codec => rubydebug
  }


  elasticsearch {
    embedded => ES_EMBEDDED
    host => "localhost"
    port => "ES_PORT"
    protocol => "ES_PROTOCOL"
  }
}

